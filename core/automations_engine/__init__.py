"""Automations engine — modular package extracted from `automation_definitions`.

The legacy module `automation_definitions` continues to be the public import
surface (façade); these submodules hold the actual implementation. Move code
here in slices to keep the public API stable.
"""
