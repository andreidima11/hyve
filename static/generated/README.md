# Generated media (local only)

ComfyUI and chat image outputs are saved here and served at `/static/generated/…`.

Bundled vendor scripts live in `vendor/` and ship with Hyve releases.

Reset: `python scripts/install_hyve.py --fresh`
