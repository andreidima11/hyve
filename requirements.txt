# Memini Bridge - runtime dependencies

fastapi>=0.128.0
uvicorn[standard]>=0.40.0
jinja2>=3.1.0
python-multipart>=0.0.20
pydantic>=2.0.0
python-dotenv>=1.0.0
PyYAML>=6.0.2

python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

sqlalchemy>=2.0.0
apscheduler>=3.10.0
chromadb>=0.5.0

httpx[http2]>=0.28.0
requests>=2.31.0
yfinance>=0.2.40
dateparser>=1.2.0
slowapi>=0.1.9

pymupdf>=1.25.0
pdfplumber>=0.11.0
trafilatura>=2.0.0
beautifulsoup4>=4.12.0
python-docx>=1.0.0
pillow>=10.0.0
sentence-transformers>=2.2.2
websockets>=12.0
firebase-admin>=6.5.0# Memini Bridge — runtime dependencies
# Instalare: pip install -r requirements.txt

# ── Core web app / API ─────────────────────────────────────────────
fastapi>=0.128.0
uvicorn[standard]>=0.40.0
jinja2>=3.1.0
python-multipart>=0.0.20
pydantic>=2.0.0
python-dotenv>=1.0.0
PyYAML>=6.0.2

# ── Auth / security ────────────────────────────────────────────────
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# ── Database / persistence ─────────────────────────────────────────
sqlalchemy>=2.0.0
apscheduler>=3.10.0
chromadb>=0.5.0

# ── HTTP / integrations ────────────────────────────────────────────
httpx[http2]>=0.28.0
requests>=2.31.0

# Yahoo Finance – pentru skill-ul yahoo_finance (cote bursă)
yfinance>=0.2.40

# Reminder avansat: parsare dată/ora din limbaj natural (mâine la 09:00, joi la 14:00, peste 2 ore)
dateparser>=1.2.0

# Rate limiting pentru API endpoints
slowapi>=0.1.9

# PDF parsing for Knowledge Base document ingestion
pymupdf>=1.25.0
pdfplumber>=0.11.0

# Main-content extraction for read_web_page (fewer nav/footer/ads in context)
trafilatura>=2.0.0

# CSS selector parsing for extract_web_data (extract specific elements from a page)
beautifulsoup4>=4.12.0

# DOCX text extraction for chat document upload
python-docx>=1.0.0

# Image processing for camera / document flows
pillow>=10.0.0

# Embedding model for memory deduplication
sentence-transformers>=2.2.2

# Home Assistant WebSocket real-time event subscription
websockets>=12.0

# Firebase Cloud Messaging server SDK
firebase-admin>=6.5.0
