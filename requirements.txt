# Hyve - runtime dependencies
# Install: pip install -r requirements.txt

# -- Core web app / API --
fastapi==0.115.6
starlette==0.41.3
uvicorn[standard]>=0.40.0
jinja2>=3.1.0
python-multipart>=0.0.20
pydantic>=2.0.0
python-dotenv>=1.0.0
PyYAML>=6.0.2

# -- Auth / security --
python-jose[cryptography]>=3.3.0
passlib>=1.7.4
bcrypt>=4.0.0

# -- Database / persistence --
sqlalchemy>=2.0.0
alembic>=1.14.0
apscheduler>=3.10.0
chromadb>=0.5.0

# -- HTTP / integrations --
httpx[http2]>=0.28.0
aiohttp>=3.9.0

# MQTT broker integration
aiomqtt>=2.0.0
paho-mqtt>=2.0.0

# Ariston NET thermostat integration
ariston>=0.19.0

# Yahoo Finance stock quotes skill
yfinance>=0.2.40

# Midea AC LAN integration
msmart-ng>=2026.4.1

# Roborock vacuum integration (cloud)
python-roborock>=5.14.1

# Mammotion lawn mower (PyMammotion; requires Python >=3.13)
pymammotion==0.8.5
betterproto2==0.9.1

# Reolink cameras / NVR (same library as Home Assistant)
reolink-aio==0.20.1

# Tapo / TP-Link Smart Home (same library as Home Assistant tplink integration)
python-kasa[speedups]==0.10.2

# Rate limiting for API endpoints
slowapi>=0.1.9

# PDF parsing for Knowledge Base
pymupdf>=1.25.0
pdfplumber>=0.11.0

# Main-content extraction for read_web_page
trafilatura>=2.0.0

# CSS selector parsing for extract_web_data
beautifulsoup4>=4.12.0

# DOCX text extraction
python-docx>=1.0.0

# Image processing
pillow>=10.0.0

# Embedding model for memory deduplication
sentence-transformers>=2.2.2

# Home Assistant WebSocket
websockets>=12.0

# Firebase Cloud Messaging server SDK
firebase-admin>=6.5.0

# Rich console logging
rich>=13.0.0
