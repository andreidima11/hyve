# Manual scripts

One-off smoke tests and operator tools. **Not collected by pytest** (`pytest.ini` limits discovery to `tests/`).

| Script | Purpose |
|--------|---------|
| `ws_notification_smoke.py` | Verify WS notification delivery against a running server |
