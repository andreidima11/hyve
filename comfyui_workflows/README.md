# ComfyUI workflow templates (local only)

Upload workflow JSON from ComfyUI (**Save → API Format**) via Settings → Integrations → ComfyUI.

Files here are per-installation and are not committed to git.

Reset: `python scripts/install_hyve.py --fresh`
